export const HORIZONTAL_NODE_SPACING = 300;
export const VERTICAL_BRANCH_SPACING = 200;
export const NODE_HEIGHT = 30;
export const NODE_WIDTH = 200;
export const BUTTON_SPACING = 50;
export const MIDDLE = BUTTON_SPACING + (NODE_WIDTH / 2) - (NODE_HEIGHT / 2);
